import React from 'react';

const Support = (props) => {
  return (
        <div>
            <h4>{props.title}</h4>
            <li>{props.text}</li>
        </div>
    );
};

export default Support;
